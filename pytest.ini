[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --disable-socket --allow-unix-socket
timeout = 60
filterwarnings =
    error::DeprecationWarning:custom_components.infinitive.*
